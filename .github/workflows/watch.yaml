name: C/C++ CI

on:
  push:
    branches: Sauske-4-CI*

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@Sauske-4-CI
    #- name: configured
      #run: ./configure
    - name: make
      run: make
    #- name: make check
    #  run: make check
    #- name: make distcheck
    #  run: make distcheck
